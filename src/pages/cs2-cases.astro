---
import Base from '../layouts/Base.astro';
import ExploreMore from '../components/ExploreMore.astro';
import { breadcrumbSchema, faqSchema } from '../lib/weapons';
import { caseDatabase } from '../lib/cases';
import type { CaseData } from '../lib/cases';
import casePricesData from '../lib/case-prices.json';
import { getSouvenirPackages, getSouvenirTournaments, STICKER_CAPSULES, AUTOGRAPH_CAPSULES, MUSIC_KIT_BOXES } from '../lib/containers';

const casePrices = (casePricesData as any).cases || {};

interface CaseItem {
  id: string;
  name: string;
  year: number;
  type: 'weapon' | 'souvenir' | 'collection';
  keyPrice: number;
  casePrice: number;
  totalCost: number;
  notableSkins: string[];
  releaseDate: string;
  image?: string;
  description?: string;
}

// Build the cases array from caseDatabase, enriched with images, descriptions, and real skin data
const dbCases: CaseItem[] = Object.values(caseDatabase).map((c: CaseData) => {
  const casePrice = casePrices[c.id]?.price || 0;
  return {
    id: c.id,
    name: c.name,
    year: c.year,
    type: c.type,
    keyPrice: c.keyPrice,
    casePrice,
    totalCost: casePrice + c.keyPrice,
    notableSkins: c.skins
      .filter((s) => s.rarity === 'covert' || s.rarity === 'classified')
      .slice(0, 3)
      .map((s) => `${s.weapon} ${s.name}`),
    releaseDate: c.releaseDate,
    image: c.image,
    description: c.description,
  };
});

// IDs already covered by caseDatabase
const dbIds = new Set(Object.keys(caseDatabase));

// Fallback cases not in caseDatabase (keep them so the page still shows all cases)
// All cases are now in caseDatabase — no fallback entries needed
const fallbackCases: CaseItem[] = [];

// Merge and sort by year descending, then by name
const cases: CaseItem[] = [...dbCases, ...fallbackCases].sort((a, b) => b.year - a.year || a.name.localeCompare(b.name));

const collections: { name: string; year: number; mapName: string }[] = [
  { name: 'Dust 2 Collection', year: 2024, mapName: 'Dust 2' },
  { name: 'Inferno Collection', year: 2024, mapName: 'Inferno' },
  { name: 'Mirage Collection', year: 2024, mapName: 'Mirage' },
  { name: 'Nuke Collection', year: 2024, mapName: 'Nuke' },
  { name: 'Anubis Collection', year: 2023, mapName: 'Anubis' },
  { name: 'Vertigo Collection', year: 2023, mapName: 'Vertigo' },
  { name: 'Ancient Collection', year: 2023, mapName: 'Ancient' },
  { name: 'Overpass Collection', year: 2023, mapName: 'Overpass' },
];

const breadcrumbs = [
  { name: 'Home', url: '/' },
  { name: 'Cases & Collections', url: '/cs2-cases/' },
];

const faqData = [
  { q: 'What exactly is a case in CS2?', a: 'Cases are loot containers available in Counter-Strike 2. Each one requires a matching key to unlock. Once you open a case, you receive a randomly selected weapon skin from that case\'s pool — the rarity tier of the item you pull determines both its visual quality and its potential market worth.' },
  { q: 'What are the drop rates for each rarity tier?', a: 'Every case follows a fixed probability table: Mil-Spec items drop at roughly 79.92%, Restricted at 15.98%, Classified at 3.2%, Covert at 0.64%, and the Special/Rare item at approximately 0.26%. Our Case Odds Calculator can help you crunch the expected return on any given case.' },
  { q: 'How are souvenir cases different from standard weapon cases?', a: 'Souvenir packages are exclusively awarded during live competitive tournament matches and carry trade restrictions in certain regions. The skins inside bear unique match details and team logo stickers, which makes them particularly desirable among collectors willing to pay a premium.' },
  { q: 'Do older cases lose their value over time?', a: 'It depends entirely on what is inside them. Key costs can fluctuate, but legacy cases frequently contain discontinued skins that have become extremely scarce. Early releases like the Arms Deal and Bravo collections, for instance, house items such as the Dragon Lore that fetch premium prices on secondary markets.' },
  { q: 'Can you actually make money by opening cases?', a: 'On average, no — the cost of keys means the expected return is negative over a large sample size. If you want to experience the thrill without the financial risk, give our Case Simulator a spin. The reality is that most players crack cases for the excitement and to chase skins they genuinely want, not as a money-making strategy.' },
  { q: 'What sets collections apart from cases?', a: 'Collections are thematic skin sets tied to specific map updates. Unlike cases, they do not require a purchased key — skins from collections are obtained through in-game drops or direct marketplace purchases. Their designs tend to reflect the visual identity of the map they are associated with.' },
  { q: 'Which case skins tend to be the most valuable?', a: 'The Covert tier consistently produces the priciest pulls — these are usually standout rifles, pistols, or sniper skins. Dragon Lore variants remain some of the most sought-after and expensive items in CS2 history, commanding five-figure prices for pristine examples.' },
  { q: 'Is it still possible to unbox skins from discontinued cases?', a: 'Absolutely. Legacy cases continue to exist in the drop system and can also be bought from the Steam Community Market or third-party platforms. Interestingly, key prices on older cases are sometimes lower, which can make them a more budget-friendly unboxing option compared to the latest releases.' },
];

const weaponCases = cases.filter(c => c.type === 'weapon');
const specialCases = cases.filter(c => c.type !== 'weapon');
const uniqueYears = [...new Set(cases.map(c => c.year))].sort((a, b) => b - a);

// Souvenir packages grouped by tournament
const souvenirPackages = getSouvenirPackages();
const souvenirTournaments = getSouvenirTournaments();

// Sticker + autograph + music kit containers
const stickerCapsules = [...STICKER_CAPSULES];
const autographCapsules = [...AUTOGRAPH_CAPSULES];
const musicKitBoxes = [...MUSIC_KIT_BOXES];
const totalContainers = cases.length + souvenirPackages.length + stickerCapsules.length + autographCapsules.length + musicKitBoxes.length;
---

<Base
  title="CS2 Case Archive — Every Weapon Case & Collection | CSGOLuck Wiki"
  description="Explore the full catalog of CS2 weapon cases and collections. Filter by category, check drop probabilities, see headline skins, and compare key prices from the original Arms Deal through the latest releases."
  currentNav="/cs2-skins/"
>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema(breadcrumbs))} />
    <script type="application/ld+json" set:html={JSON.stringify(faqSchema(faqData))} />
  </Fragment>

  <!-- Breadcrumb -->
  <nav class="ch-breadcrumb">
    <a href="/">Home</a> <span class="ch-sep">/</span> <span>Cases & Collections</span>
  </nav>

  <!-- Hero -->
  <section class="ch-hero">
    <div class="ch-hero-text">
      <h1 class="ch-hero-title">CS2 Case Archive</h1>
      <p class="ch-hero-desc">
        The complete index of CS2 weapon cases and map collections. Check the <a href="/case-odds-calculator/">drop probabilities</a> before you open, or fire up the <a href="/case-simulator/">Case Simulator</a> risk-free.
      </p>
    </div>
    <div class="ch-hero-pills">
      <span class="ch-pill"><strong>{totalContainers}</strong> Containers</span>
      <span class="ch-pill"><strong>{souvenirPackages.length}</strong> Souvenirs</span>
      <span class="ch-pill"><strong>{stickerCapsules.length + autographCapsules.length}</strong> Capsules</span>
    </div>
  </section>

  <!-- Filter Tabs -->
  <div class="ch-tabs" id="ch-tabs">
    <button class="ch-tab ch-tab-active" data-filter="weapon">Weapon Cases ({cases.length})</button>
    <button class="ch-tab" data-filter="souvenirs">Souvenirs ({souvenirPackages.length})</button>
    <button class="ch-tab" data-filter="stickers">Sticker Capsules ({stickerCapsules.length})</button>
    <button class="ch-tab" data-filter="autographs">Autographs ({autographCapsules.length})</button>
    <button class="ch-tab" data-filter="music">Music Kits ({musicKitBoxes.length})</button>
    <button class="ch-tab" data-filter="collections">Collections</button>
  </div>

  <!-- Cases Grid -->
  <div id="ch-cases-section" class="ch-grid">
    {cases.map((caseItem) => (
      <a href={`/cs2-cases/${caseItem.id}/`} class="ch-card" data-type={caseItem.type === 'weapon' ? 'weapon' : 'special'}>
        <div class="ch-card-img">
          {caseItem.image ? (
            <img src={caseItem.image} alt={caseItem.name} loading="lazy" width="200" height="150" />
          ) : (
            <span class="ch-card-placeholder"></span>
          )}
        </div>
        <div class="ch-card-body">
          <span class="ch-card-name">{caseItem.name}</span>
          <span class="ch-card-year">{caseItem.year}</span>
          {caseItem.notableSkins.length > 0 && (
            <span class="ch-card-rare">{caseItem.notableSkins[0]}</span>
          )}
          {caseItem.totalCost > 0 && <span class="ch-card-price">${caseItem.totalCost.toFixed(2)} to open</span>}
        </div>
      </a>
    ))}
  </div>

  <!-- Souvenir Packages Grid -->
  <div id="ch-souvenirs-section" class="ch-section-wrap" style="display:none;">
    {souvenirTournaments.map((tournament) => {
      const pkgs = souvenirPackages.filter(p => p.tournament === tournament);
      return (
        <div class="ch-tournament-group">
          <h3 class="ch-tournament-title">{tournament}</h3>
          <div class="ch-grid">
            {pkgs.map((pkg) => (
              <div class="ch-card ch-card-souvenir">
                <div class="ch-card-img ch-card-img-souvenir">
                  <span class="ch-card-map-label">{pkg.map}</span>
                </div>
                <div class="ch-card-body">
                  <span class="ch-card-name">{pkg.name}</span>
                  <span class="ch-card-year">{pkg.year} &middot; {pkg.collection}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      );
    })}
  </div>

  <!-- Sticker Capsules Grid -->
  <div id="ch-stickers-section" class="ch-grid" style="display:none;">
    {stickerCapsules.map((cap) => (
      <div class="ch-card ch-card-sticker">
        <div class="ch-card-img ch-card-img-capsule">
          <span class="ch-card-cap-icon">S</span>
        </div>
        <div class="ch-card-body">
          <span class="ch-card-name">{cap.name}</span>
          {cap.tournament && <span class="ch-card-year">{cap.tournament}</span>}
          {!cap.tournament && cap.year && <span class="ch-card-year">{cap.year}</span>}
        </div>
      </div>
    ))}
  </div>

  <!-- Autograph Capsules Grid -->
  <div id="ch-autographs-section" class="ch-grid" style="display:none;">
    {autographCapsules.map((cap) => (
      <div class="ch-card ch-card-autograph">
        <div class="ch-card-img ch-card-img-capsule">
          <span class="ch-card-cap-icon">A</span>
        </div>
        <div class="ch-card-body">
          <span class="ch-card-name">{cap.name}</span>
          {cap.tournament && <span class="ch-card-year">{cap.tournament}</span>}
        </div>
      </div>
    ))}
  </div>

  <!-- Music Kit Boxes Grid -->
  <div id="ch-music-section" class="ch-grid" style="display:none;">
    {musicKitBoxes.map((kit) => (
      <div class="ch-card ch-card-music">
        <div class="ch-card-img ch-card-img-capsule">
          <span class="ch-card-cap-icon">M</span>
        </div>
        <div class="ch-card-body">
          <span class="ch-card-name">{kit.name}</span>
        </div>
      </div>
    ))}
  </div>

  <!-- Collections Grid -->
  <div id="ch-collections-section" class="ch-grid" style="display:none;">
    {collections.map((coll) => (
      <a href={`/cs2-maps/${coll.mapName.toLowerCase().replace(/ /g, '-')}/`} class="ch-card">
        <div class="ch-card-img ch-card-img-coll">
          <span class="ch-card-map-label">{coll.mapName}</span>
        </div>
        <div class="ch-card-body">
          <span class="ch-card-name">{coll.name}</span>
          <span class="ch-card-year">{coll.year}</span>
        </div>
      </a>
    ))}
  </div>

  <!-- FAQ Accordion -->
  <section class="ch-faq">
    <h2 class="ch-faq-title">Frequently Asked Questions</h2>
    {faqData.map((faq) => (
      <details class="ch-faq-item">
        <summary class="ch-faq-q">{faq.q}</summary>
        <p class="ch-faq-a">{faq.a}</p>
      </details>
    ))}
  </section>

  <!-- Internal Links -->
  <div class="ch-links">
    <a href="/case-simulator/">Case Simulator</a>
    <a href="/case-odds-calculator/">Case Odds Calculator</a>
    <a href="/cs2-skins/">Skin Database</a>
    <a href="/skin-compare/">Skin Compare</a>
    <a href="/price-tracker/">Price Tracker</a>
    <a href="/market-trends/">Market Trends</a>
    <a href="/float-checker/">Float Checker</a>
    <a href="/tradeup-calculator/">Trade-Up Calculator</a>
    <a href="/inventory-lookup/">Inventory Lookup</a>
    <a href="/loadout-builder/">Loadout Builder</a>
    <a href="/cs2-weapons/">Weapon Database</a>
    <a href="/cs2-guides/skins-investing-guide/">Investing Guide</a>
    <a href="/cs2-guides/trading-guide/">Trading Guide</a>
    <a href="/cs2-stickers/">Sticker Database</a>
    <a href="/cs2-agents/">Agents & Music Kits</a>
    <a href="/cs2-guides/float-guide/">Float Guide</a>
    <a href="/cs2-guides/case-hardened-guide/">Case Hardened Guide</a>
  </div>

  <ExploreMore context="skins" />

  <style is:inline>
    :root {
      --accent: #2ecc71;
      --bg2: #13141a;
      --bg3: #1a1c24;
      --border: #242630;
    }

    /* ---- BREADCRUMB ---- */
    .ch-breadcrumb {
      display: flex;
      align-items: center;
      gap: 0.35rem;
      margin-bottom: 1.25rem;
      font-size: 0.82rem;
    }

    .ch-breadcrumb a {
      color: var(--text2);
      text-decoration: none;
    }

    .ch-breadcrumb a:hover {
      color: var(--accent);
    }

    .ch-sep {
      color: var(--border);
    }

    .ch-breadcrumb > span:last-child {
      color: var(--text);
      font-weight: 600;
    }

    /* ---- HERO ---- */
    .ch-hero {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 1.5rem;
      margin-bottom: 2rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid var(--border);
    }

    .ch-hero-text {
      flex: 1;
    }

    .ch-hero-title {
      font-size: 1.75rem;
      font-weight: 700;
      color: var(--text);
      margin: 0 0 0.5rem;
      letter-spacing: -0.02em;
    }

    .ch-hero-desc {
      font-size: 0.92rem;
      color: var(--text2);
      margin: 0;
      line-height: 1.6;
      max-width: 540px;
    }

    .ch-hero-desc a {
      color: var(--accent);
      font-weight: 600;
      text-decoration: none;
    }

    .ch-hero-desc a:hover {
      text-decoration: underline;
    }

    .ch-hero-pills {
      display: flex;
      gap: 0.5rem;
      flex-shrink: 0;
    }

    .ch-pill {
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
      background: var(--bg2);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 0.45rem 0.75rem;
      font-size: 0.8rem;
      color: var(--text2);
    }

    .ch-pill strong {
      color: var(--accent);
      font-weight: 700;
      font-size: 0.92rem;
    }

    /* ---- FILTER TABS ---- */
    .ch-tabs {
      display: flex;
      gap: 0.35rem;
      margin-bottom: 1.25rem;
      flex-wrap: wrap;
    }

    .ch-tab {
      background: var(--bg2);
      border: 1px solid var(--border);
      border-radius: 4px;
      padding: 0.4rem 0.85rem;
      font-size: 0.82rem;
      font-weight: 500;
      color: var(--text2);
      cursor: pointer;
      font-family: inherit;
      transition: color 0.15s, border-color 0.15s;
    }

    .ch-tab:hover {
      color: var(--text);
      border-color: var(--accent);
    }

    .ch-tab-active {
      color: var(--accent);
      border-color: var(--accent);
      font-weight: 600;
    }

    /* ---- CARD GRID ---- */
    .ch-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(210px, 1fr));
      gap: 0.75rem;
      margin-bottom: 2rem;
    }

    .ch-card {
      display: flex;
      flex-direction: column;
      background: var(--bg2);
      border: 1px solid var(--border);
      border-radius: 8px;
      overflow: hidden;
      text-decoration: none;
      color: inherit;
      transition: transform 0.2s, border-color 0.2s, box-shadow 0.2s;
    }

    .ch-card:hover {
      transform: translateY(-4px);
      border-color: var(--accent);
      box-shadow: 0 8px 24px rgba(0,0,0,0.3);
    }

    .ch-card-img {
      height: 140px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--bg3);
      overflow: hidden;
    }

    .ch-card-img img {
      max-width: 85%;
      max-height: 120px;
      object-fit: contain;
      transition: transform 0.25s;
    }

    .ch-card:hover .ch-card-img img {
      transform: scale(1.06);
    }

    .ch-card-placeholder {
      width: 60px;
      height: 60px;
      background: var(--border);
      border-radius: 8px;
      opacity: 0.2;
    }

    .ch-card-img-coll {
      background: linear-gradient(135deg, var(--bg3), var(--bg2));
    }

    .ch-card-img-souvenir {
      background: linear-gradient(135deg, rgba(46,204,113,.08), var(--bg3));
      border-bottom: 2px solid rgba(46,204,113,.2);
    }

    .ch-card-img-capsule {
      background: linear-gradient(135deg, rgba(136,71,255,.08), var(--bg3));
      border-bottom: 2px solid rgba(136,71,255,.15);
    }

    .ch-card-cap-icon {
      width: 48px; height: 48px;
      display: flex; align-items: center; justify-content: center;
      background: rgba(136,71,255,.12);
      border: 1px solid rgba(136,71,255,.2);
      border-radius: 50%;
      font-size: 1.2rem; font-weight: 700;
      color: #8847ff;
    }

    .ch-card-music .ch-card-cap-icon {
      background: rgba(245,166,35,.12);
      border-color: rgba(245,166,35,.2);
      color: #f5a623;
    }

    .ch-card-music .ch-card-img-capsule {
      background: linear-gradient(135deg, rgba(245,166,35,.08), var(--bg3));
      border-bottom-color: rgba(245,166,35,.15);
    }

    .ch-tournament-group { margin-bottom: 1.5rem; }
    .ch-tournament-title {
      font-size: .95rem; font-weight: 700; color: var(--text);
      margin: 0 0 .5rem; padding-left: .6rem;
      border-left: 3px solid var(--accent);
    }

    .ch-section-wrap { margin-bottom: 2rem; }

    .ch-card-map-label {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--text2);
      opacity: 0.4;
    }

    .ch-card-body {
      padding: 0.6rem 0.7rem;
      display: flex;
      flex-direction: column;
      gap: 0.1rem;
      flex: 1;
    }

    .ch-card-name {
      font-size: 0.85rem;
      font-weight: 700;
      color: var(--text);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .ch-card-year {
      font-size: 0.68rem;
      color: var(--text2);
    }

    .ch-card-rare {
      font-size: 0.72rem;
      color: var(--text2);
      opacity: 0.75;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      margin-top: 0.15rem;
    }

    .ch-card-price {
      font-size: 0.78rem;
      font-weight: 700;
      color: var(--accent);
      margin-top: auto;
      padding-top: 0.15rem;
    }

    /* ---- FAQ ACCORDION ---- */
    .ch-faq {
      margin: 2rem 0;
    }

    .ch-faq-title {
      font-size: 1.15rem;
      font-weight: 700;
      color: var(--text);
      margin: 0 0 1rem;
      padding-left: 0.85rem;
      border-left: 3px solid var(--accent);
    }

    .ch-faq-item {
      border-bottom: 1px solid var(--border);
    }

    .ch-faq-item[open] {
      padding-bottom: 0.75rem;
    }

    .ch-faq-q {
      font-size: 0.92rem;
      font-weight: 600;
      color: var(--text);
      padding: 0.75rem 2.5rem 0.75rem 0;
      cursor: pointer;
      list-style: none;
      position: relative;
      transition: color 0.15s;
    }

    .ch-faq-q::-webkit-details-marker {
      display: none;
    }

    .ch-faq-q::after {
      content: '+';
      position: absolute;
      right: 1.25rem;
      top: 50%;
      transform: translateY(-50%);
      font-size: 1.2rem;
      color: var(--accent);
      font-weight: 700;
    }

    .ch-faq-item[open] .ch-faq-q::after {
      content: '-';
    }

    .ch-faq-q:hover {
      color: var(--accent);
    }

    .ch-faq-a {
      font-size: 0.88rem;
      color: var(--text2);
      line-height: 1.6;
      margin: 0;
      padding-left: 0;
    }

    .ch-faq-a a {
      color: var(--accent);
      text-decoration: none;
      font-weight: 600;
    }

    .ch-faq-a a:hover {
      text-decoration: underline;
    }

    /* ---- INTERNAL LINKS ---- */
    .ch-links {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
      margin: 1.5rem 0;
    }

    .ch-links a {
      font-size: 0.8rem;
      color: var(--text2);
      text-decoration: none;
      padding: 0.3rem 0.6rem;
      background: var(--bg2);
      border: 1px solid var(--border);
      border-radius: 4px;
      border-left: 3px solid transparent;
      transition: border-left-color 0.15s, color 0.15s;
    }

    .ch-links a:hover {
      border-left-color: var(--accent);
      color: var(--accent);
    }

    /* ---- RESPONSIVE ---- */
    @media (max-width: 768px) {
      .ch-hero {
        flex-direction: column;
        gap: 1rem;
      }

      .ch-hero-pills {
        flex-wrap: wrap;
      }
    }

    @media (max-width: 640px) {
      .ch-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 0.5rem;
      }

      .ch-card-img {
        height: 100px;
      }

      .ch-card-img img {
        max-height: 85px;
      }

      .ch-card-body {
        padding: 0.45rem 0.5rem;
      }

      .ch-card-name {
        font-size: 0.78rem;
      }
    }
  </style>

  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const tabs = document.querySelectorAll('.ch-tab');
      const sections = {
        weapon: document.getElementById('ch-cases-section'),
        souvenirs: document.getElementById('ch-souvenirs-section'),
        stickers: document.getElementById('ch-stickers-section'),
        autographs: document.getElementById('ch-autographs-section'),
        music: document.getElementById('ch-music-section'),
        collections: document.getElementById('ch-collections-section'),
      };

      function showSection(key) {
        for (const [k, el] of Object.entries(sections)) {
          if (!el) continue;
          el.style.display = k === key ? (k === 'souvenirs' ? 'block' : 'grid') : 'none';
        }
      }

      tabs.forEach(tab => {
        tab.addEventListener('click', () => {
          tabs.forEach(t => t.classList.remove('ch-tab-active'));
          tab.classList.add('ch-tab-active');
          showSection(tab.getAttribute('data-filter'));
        });
      });
    });
  </script>
</Base>
