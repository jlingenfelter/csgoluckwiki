---
import Base from '../layouts/Base.astro';
import ExploreMore from '../components/ExploreMore.astro';
import { faqSchema, breadcrumbSchema } from '../lib/weapons';

const title = 'CS2 Steam Inventory Value Calculator | CSGOLuck Wiki';
const description = 'Check your Steam inventory CS2 skin value instantly. Get total inventory worth, item breakdown by category, and find your most valuable skins. Free steam inventory value checker for Counter-Strike 2.';

const breadcrumbs = [
  { name: 'Home', url: '/' },
  { name: 'Skins', url: '/cs2-skins/' },
  { name: 'Inventory Lookup', url: '/inventory-lookup/' },
];

const faqs = [
  {
    question: 'How do I find my Steam ID?',
    answer: 'You can find your Steam ID in several ways: (1) Visit your Steam profile, copy the URL (you\'ll see /profiles/XXXXX or /id/vanityname), (2) Use a Steam ID lookup tool, (3) In the lookup form, you can paste your full profile URL or just your username. Our tool accepts all three formats.'
  },
  {
    question: 'Is my inventory information private?',
    answer: 'Your inventory must be set to public for us to access it via the Steam Web API. The lookup is done entirely client-side — we don\'t store any of your data. You can make your inventory private again after checking your value.'
  },
  {
    question: 'Why can\'t I see all my items?',
    answer: 'This can happen if: (1) Your inventory is set to private, (2) You have too many items (Steam limits API returns), (3) Your profile is not public. Make sure your profile and inventory are both set to public in your Steam privacy settings.'
  },
  {
    question: 'How often are prices updated?',
    answer: 'Prices are pulled from current market data and reflect the latest available pricing information. For the most accurate current values, check the individual skin listing on our Skin Database or PriceEmpire.'
  },
  {
    question: 'What counts toward inventory value?',
    answer: 'Only CS2 weapon skins, knives, gloves, and agent skins count toward your inventory value. Cases, keys, stickers, music kits, and other non-weapon items are not included in the total value calculation.'
  },
  {
    question: 'Can I compare my inventory to other players?',
    answer: 'Yes! Look up anyone\'s Steam ID to see their public inventory value. You can use this to compare collections with friends or research what top collectors own.'
  },
];
---

<Base title={title} description={description} currentNav="/cs2-skins/">
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema(breadcrumbs))} />
    <script type="application/ld+json" set:html={JSON.stringify(faqSchema(faqs))} />
  </Fragment>

  <!-- Breadcrumb -->
  <nav class="il-breadcrumb">
    <a href="/">Home</a> / <a href="/cs2-skins/">Skins</a> / <span>Inventory Lookup</span>
  </nav>

  <!-- Hero Section -->
  <section class="il-hero">
    <h1>CS2 Steam Inventory Value Calculator</h1>
    <p>Instantly calculate your total inventory value and break down your collection by weapon category. Look up any public Steam inventory.</p>
  </section>

  <!-- Lookup Form -->
  <section class="il-lookup">
    <h2>Look Up Inventory</h2>
    <p>Enter your Steam ID, profile URL, or vanity username to analyze your inventory value.</p>

    <div class="il-form">
      <div class="il-form-group">
        <label for="steamInput">Steam ID / Profile URL / Username:</label>
        <input
          type="text"
          id="steamInput"
          placeholder="e.g., 76561198000000000 or username or https://steamcommunity.com/id/username"
          autocomplete="off"
        >
        <small>Your inventory must be set to public in Steam privacy settings</small>
      </div>
      <button id="lookupBtn" class="il-lookup-btn">Look Up Inventory</button>
    </div>

    <!-- Loading State -->
    <div id="loadingState" class="il-loading" style="display:none">
      <div class="il-spinner"></div>
      <p>Fetching your inventory...</p>
    </div>

    <!-- Error State -->
    <div id="errorState" class="il-error" style="display:none">
      <p id="errorMessage"></p>
    </div>

    <!-- Results Section -->
    <div id="resultsSection" class="il-results" style="display:none">
      <!-- Summary Stats -->
      <div class="il-stats">
        <div class="il-stat">
          <div class="il-stat-val" id="totalValue">$0.00</div>
          <div class="il-stat-label">Total Value</div>
        </div>
        <div class="il-stat">
          <div class="il-stat-val" id="itemCount">0</div>
          <div class="il-stat-label">Items</div>
        </div>
        <div class="il-stat">
          <div class="il-stat-val" id="mostValuableItem">—</div>
          <div class="il-stat-label">Most Valuable</div>
        </div>
        <div class="il-stat">
          <div class="il-stat-val" id="avgPrice">$0.00</div>
          <div class="il-stat-label">Avg Price</div>
        </div>
      </div>

      <!-- Portfolio Grade -->
      <div class="il-grade" id="portfolioGrade" style="display:none">
        <div class="il-grade-badge" id="gradeBadge">-</div>
        <div class="il-grade-info">
          <div class="il-grade-label">Portfolio Grade</div>
          <div class="il-grade-desc" id="gradeDesc">-</div>
        </div>
      </div>

      <!-- Category Breakdown -->
      <h3>Inventory by Category</h3>
      <div class="il-breakdown" id="categoryBreakdown"></div>

      <!-- Rarity Breakdown -->
      <div id="raritySection" style="display:none">
        <h3>Price Tier Distribution</h3>
        <div class="il-rarity-breakdown" id="rarityBreakdown"></div>
      </div>

      <!-- Top Items -->
      <h3>Most Valuable Items</h3>
      <div class="il-items" id="topItems"></div>

      <!-- Actions -->
      <div class="il-actions">
        <button class="il-action-btn" id="copyValueBtn">Copy Total Value</button>
        <a href="/cs2-skins/" class="il-action-btn">Browse All Skins</a>
        <a href="/market-trends/" class="il-action-btn">Market Trends</a>
      </div>
    </div>
  </section>

  <!-- How to Find Steam ID -->
  <section class="il-guide">
    <h2>How to Find Your Steam ID</h2>
    <div class="il-guide-list">
      <div class="il-guide-row">
        <h3>Method 1: From Profile URL</h3>
        <p>Visit your Steam profile and look at the URL bar. Your Steam ID is the long number after /profiles/ or your username after /id/</p>
        <code class="il-code">https://steamcommunity.com/profiles/76561198000000000</code>
      </div>
      <div class="il-guide-row">
        <h3>Method 2: Vanity Username</h3>
        <p>If you set a custom URL, use just your username. We'll convert it to your numeric Steam ID automatically.</p>
        <code class="il-code">https://steamcommunity.com/id/myusername</code>
      </div>
      <div class="il-guide-row">
        <h3>Method 3: SteamID Tools</h3>
        <p>Use online SteamID lookup tools if you don't have your ID handy. Search "SteamID finder" to find your numeric ID in seconds.</p>
      </div>
      <div class="il-guide-row">
        <h3>Privacy Settings</h3>
        <p>Make sure your profile is public and your inventory is visible. Go to Profile → Privacy Settings and set both to Public, then try again.</p>
      </div>
    </div>
  </section>

  <!-- Related Tools -->
  <section class="il-related">
    <h2>Related Tools & Resources</h2>
    <div class="il-related-list">
      <a href="/cs2-skins/" class="il-related-row">Skin Database</a>
      <a href="/skin-compare/" class="il-related-row">Compare Skins</a>
      <a href="/price-tracker/" class="il-related-row">Price Tracker</a>
      <a href="/market-trends/" class="il-related-row">Market Trends</a>
      <a href="/float-checker/" class="il-related-row">Float Checker</a>
      <a href="/tradeup-calculator/" class="il-related-row">Trade-Up Calculator</a>
      <a href="/case-odds-calculator/" class="il-related-row">Case Odds Calculator</a>
      <a href="/case-simulator/" class="il-related-row">Case Simulator</a>
      <a href="/cs2-cases/" class="il-related-row">Cases & Collections</a>
      <a href="/loadout-builder/" class="il-related-row">Loadout Builder</a>
      <a href="/cs2-weapons/" class="il-related-row">Weapon Database</a>
      <a href="/cs2-guides/trading-guide/" class="il-related-row">Trading Guide</a>
      <a href="/cs2-guides/float-guide/" class="il-related-row">Float Guide</a>
      <a href="/cs2-guides/skins-investing-guide/" class="il-related-row">Investing Guide</a>
    </div>
  </section>

  <div class="csgoluck-cta" style="margin:2rem 0;background:linear-gradient(135deg,#1f222b 0%,#0d1a14 100%);border:1px solid rgba(46,204,113,0.2);border-radius:4px;overflow:hidden">
  <div style="display:flex;align-items:center;gap:1rem;padding:1.25rem 1.5rem;flex-wrap:wrap">
  <span style="font-size:1.5rem">🎯</span>
  <div style="flex:1;min-width:200px"><strong style="display:block;color:#fff;font-size:.95rem">Got Skins? Try Your Luck</strong><span style="color:#909090;font-size:.82rem">Use your skins on CSGOLuck.com</span></div>
  <a href="/go/" rel="nofollow" style="display:inline-flex;align-items:center;gap:.5rem;padding:.55rem 1.25rem;background:#2ecc71;color:#fff;border-radius:4px;font-weight:700;font-size:.85rem;text-decoration:none;white-space:nowrap">Visit CSGOLuck →</a>
  </div>
  </div>

  <!-- FAQ -->
  <section class="il-faq">
    <h2>Frequently Asked Questions</h2>
    {faqs.map(f => (
      <details class="il-faq-item">
        <summary>{f.question}</summary>
        <p>{f.answer}</p>
      </details>
    ))}
  </section>

  <ExploreMore context="skins" />
</Base>

<style is:inline>
/* ── Breadcrumb ── */
.il-breadcrumb{font-size:.82rem;color:var(--text2);margin-bottom:1.25rem;display:flex;gap:.35rem;flex-wrap:wrap}
.il-breadcrumb a{color:var(--accent);text-decoration:none}
.il-breadcrumb a:hover{text-decoration:underline}

/* ── Hero ── */
.il-hero{background:var(--bg2);border:1px solid var(--border);border-radius:4px;padding:2rem;margin-bottom:1.5rem}
.il-hero h1{font-family:'Montserrat',sans-serif;font-size:2rem;font-weight:800;margin:0 0 .5rem;color:var(--text)}
.il-hero p{color:var(--text2);font-size:.95rem;line-height:1.6;margin:0}

/* ── Lookup Section ── */
.il-lookup{margin-bottom:2rem}
.il-lookup h2{font-family:'Montserrat',sans-serif;font-size:1.15rem;font-weight:700;color:var(--text);margin:0 0 .5rem}
.il-lookup>p{color:var(--text2);font-size:.95rem;margin-bottom:1rem}

.il-form{display:flex;flex-direction:column;gap:1rem;margin:1.5rem 0}
.il-form-group{display:flex;flex-direction:column;gap:.5rem}
.il-form-group label{font-weight:600;color:var(--text)}
.il-form-group input{background:var(--bg3);color:var(--text);border:1px solid var(--border);padding:.75rem 1rem;border-radius:4px;font-size:.95rem;transition:border-color .2s;font-family:inherit}
.il-form-group input:focus{outline:none;border-color:var(--accent)}
.il-form-group small{font-size:.8rem;color:var(--text2)}

.il-lookup-btn{background:var(--accent);color:#000;border:none;padding:.75rem 1.5rem;border-radius:4px;font-weight:700;font-size:1rem;cursor:pointer;transition:background .2s;align-self:flex-start;font-family:inherit}
.il-lookup-btn:hover{background:var(--accent2)}

.il-loading{text-align:center;padding:2rem}
.il-spinner{width:40px;height:40px;border:3px solid var(--bg3);border-top-color:var(--accent);border-radius:50%;animation:il-spin .8s linear infinite;margin:0 auto 1rem}
@keyframes il-spin{to{transform:rotate(360deg)}}

.il-error{background:rgba(239,83,80,0.1);border:1px solid rgba(239,83,80,0.5);border-radius:4px;padding:1rem;margin:1rem 0;color:#ffb3ad}

.il-results{margin-top:2rem}

/* ── Stats ── */
.il-stats{display:flex;gap:2px;flex-wrap:wrap;margin-bottom:1.5rem}
.il-stat{flex:1;min-width:120px;background:var(--bg2);border:1px solid var(--border);border-radius:4px;padding:1rem;text-align:center}
.il-stat-val{font-family:'Montserrat',sans-serif;font-size:1.4rem;font-weight:800;color:var(--accent)}
.il-stat-label{font-size:.78rem;color:var(--text2);text-transform:uppercase;letter-spacing:.04em;font-weight:600;margin-top:.25rem}

/* ── Grade ── */
.il-grade{display:flex;align-items:center;gap:1.25rem;background:var(--bg2);border:1px solid var(--border);border-radius:4px;padding:1.25rem 1.5rem;margin:1.5rem 0}
.il-grade-badge{font-size:2rem;font-weight:900;width:60px;height:60px;display:flex;align-items:center;justify-content:center;border-radius:4px;background:var(--bg3);border:2px solid var(--border);flex-shrink:0}
.il-grade-label{font-size:.82rem;color:var(--text2);text-transform:uppercase;letter-spacing:.05em;font-weight:700;margin-bottom:.25rem}
.il-grade-desc{font-size:.9rem;color:var(--text);line-height:1.4}

/* ── Breakdown ── */
.il-breakdown{display:flex;flex-direction:column;gap:2px;margin:1rem 0 2rem}
.il-category-card{background:var(--bg2);border:1px solid var(--border);border-left:3px solid transparent;border-radius:4px;padding:.75rem 1rem;display:flex;align-items:center;gap:1rem;transition:border-color .15s,background .15s}
.il-category-card:hover{border-left-color:var(--accent);background:var(--bg3)}
.il-category-card .il-cat-count{font-size:1.1rem;font-weight:800;color:var(--accent);min-width:40px}
.il-category-card .il-cat-name{font-weight:600;color:var(--text);flex:1}
.il-category-card .il-cat-value{font-size:.9rem;color:var(--text2)}

/* ── Rarity Breakdown ── */
.il-rarity-breakdown{display:flex;flex-direction:column;gap:.5rem;margin:1rem 0 2rem;background:var(--bg2);border:1px solid var(--border);border-radius:4px;padding:1.25rem}
.il-tier-row{display:grid;grid-template-columns:100px 1fr 80px;gap:.75rem;align-items:center}
.il-tier-label{font-size:.82rem;font-weight:600;color:var(--text)}
.il-tier-bar-wrap{height:22px;background:var(--bg3);border-radius:4px;overflow:hidden}
.il-tier-bar{height:100%;border-radius:4px;transition:width .3s ease}
.il-tier-count{font-size:.82rem;font-weight:700;text-align:right}

/* ── Items ── */
.il-items{display:flex;flex-direction:column;gap:2px;margin:1rem 0 2rem}
.il-item-card{display:flex;align-items:center;gap:1rem;background:var(--bg2);border:1px solid var(--border);border-left:3px solid transparent;border-radius:4px;padding:.75rem 1rem;text-decoration:none;color:inherit;transition:border-color .15s,background .15s}
.il-item-card:hover{border-left-color:var(--accent);background:var(--bg3)}
.il-item-img{width:60px;height:45px;background:var(--bg3);border-radius:4px;display:flex;align-items:center;justify-content:center;color:var(--text2);font-size:.7rem;flex-shrink:0;overflow:hidden}
.il-item-info{flex:1;min-width:0}
.il-item-name{font-weight:700;color:var(--text);font-size:.9rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.il-item-wear{font-size:.78rem;color:var(--text2)}
.il-item-price{font-size:1rem;font-weight:800;color:var(--accent);flex-shrink:0}

/* ── Actions ── */
.il-actions{display:flex;gap:.75rem;flex-wrap:wrap;margin:2rem 0;padding-top:1.5rem;border-top:1px solid var(--border)}
.il-action-btn{background:var(--bg3);color:var(--text);border:1px solid var(--border);padding:.6rem 1.25rem;border-radius:4px;font-size:.88rem;font-weight:600;cursor:pointer;text-decoration:none;transition:border-color .15s,color .15s;font-family:inherit}
.il-action-btn:hover{border-color:var(--accent);color:var(--accent)}

/* ── Guide Section ── */
.il-guide{margin-bottom:2rem}
.il-guide h2{font-family:'Montserrat',sans-serif;font-size:1.15rem;font-weight:700;color:var(--text);margin:0 0 .75rem}
.il-guide-list{display:flex;flex-direction:column;gap:2px}
.il-guide-row{background:var(--bg2);border:1px solid var(--border);border-left:3px solid transparent;border-radius:4px;padding:1rem 1.25rem;transition:border-color .15s,background .15s}
.il-guide-row:hover{border-left-color:var(--accent);background:var(--bg3)}
.il-guide-row h3{font-family:'Montserrat',sans-serif;color:var(--text);font-size:1rem;font-weight:700;margin:0 0 .35rem}
.il-guide-row p{color:var(--text2);font-size:.9rem;line-height:1.5;margin:0 0 .5rem}
.il-code{background:var(--bg3);padding:.5rem;border-radius:4px;display:block;font-size:.8rem;color:var(--text2)}

/* ── Related ── */
.il-related{margin-bottom:2rem}
.il-related h2{font-family:'Montserrat',sans-serif;font-size:1.15rem;font-weight:700;color:var(--text);margin:0 0 .75rem}
.il-related-list{display:flex;flex-direction:column;gap:2px}
.il-related-row{display:flex;align-items:center;text-decoration:none;color:var(--text);background:var(--bg2);border:1px solid var(--border);border-left:3px solid transparent;border-radius:4px;padding:.6rem 1rem;font-size:.9rem;font-weight:600;transition:border-color .15s,background .15s}
.il-related-row:hover{border-left-color:var(--accent);background:var(--bg3)}

/* ── FAQ ── */
.il-faq{margin-bottom:2rem}
.il-faq h2{font-family:'Montserrat',sans-serif;font-size:1.15rem;font-weight:700;color:var(--text);margin:0 0 .75rem}
.il-faq-item{background:var(--bg2);border:1px solid var(--border);border-radius:4px;margin-bottom:.35rem;overflow:hidden}
.il-faq-item summary{padding:.75rem 2.5rem .75rem 1rem;font-weight:700;font-size:.9rem;color:var(--text);cursor:pointer;list-style:none;position:relative;transition:background .15s}
.il-faq-item summary::after{content:'+';position:absolute;right:1.25rem;top:50%;transform:translateY(-50%);font-size:1.2rem;color:var(--accent);font-weight:700}
.il-faq-item[open] summary::after{content:'\2212'}
.il-faq-item summary:hover{background:var(--bg3)}
.il-faq-item summary::-webkit-details-marker{display:none}
.il-faq-item p{padding:0 1rem .85rem 1rem;margin:0;color:var(--text2);font-size:.88rem;line-height:1.6}

/* ── Responsive ── */
@media(max-width:768px){
  .il-hero h1{font-size:1.5rem}
  .il-form{flex-direction:column}
  .il-lookup-btn{width:100%}
  .il-stats{flex-direction:column}
  .il-tier-row{grid-template-columns:80px 1fr 60px}
}
</style>

<script is:inline>
  (function() {
    const steamInput = document.getElementById('steamInput');
    const lookupBtn = document.getElementById('lookupBtn');
    const loadingState = document.getElementById('loadingState');
    const errorState = document.getElementById('errorState');
    const errorMessage = document.getElementById('errorMessage');
    const resultsSection = document.getElementById('resultsSection');

    lookupBtn.addEventListener('click', performLookup);
    steamInput.addEventListener('keypress', function(e) {
      if (e.key === 'Enter') performLookup();
    });

    async function performLookup() {
      const input = steamInput.value.trim();
      if (!input) {
        showError('Please enter a Steam ID, profile URL, or username');
        return;
      }

      loadingState.style.display = 'block';
      errorState.style.display = 'none';
      resultsSection.style.display = 'none';

      try {
        // Parse input to get steamid
        let steamid = parseSteamInput(input);

        if (!steamid) {
          throw new Error('Could not parse Steam ID. Please check your input format.');
        }

        // Fetch inventory data
        const response = await fetch(`/api/inventory?steamid=${encodeURIComponent(steamid)}`);
        const contentType = response.headers.get('content-type') || '';
        if (!contentType.includes('application/json')) {
          throw new Error('The inventory API is currently unavailable. Please try again later.');
        }
        const data = await response.json();

        if (!response.ok || data.error) {
          throw new Error(data.error || 'Failed to fetch inventory');
        }

        displayResults(data);
      } catch (err) {
        showError(err.message);
      } finally {
        loadingState.style.display = 'none';
      }
    }

    function parseSteamInput(input) {
      // Check if it's a numeric Steam ID
      if (/^\d{17}$/.test(input)) {
        return input;
      }

      // Check if it's a profile URL
      const profileMatch = input.match(/\/profiles\/(\d+)/);
      if (profileMatch) {
        return profileMatch[1];
      }

      // Check if it's a vanity URL
      const vanityMatch = input.match(/\/id\/([a-zA-Z0-9_-]+)/);
      if (vanityMatch) {
        return vanityMatch[1];
      }

      // Assume it's a username if it looks reasonable
      if (/^[a-zA-Z0-9_-]{3,}$/.test(input)) {
        return input;
      }

      return null;
    }

    function displayResults(data) {
      // Update stat cards
      document.getElementById('totalValue').textContent = '$' + data.totalValue.toFixed(2);
      document.getElementById('itemCount').textContent = data.items.length;
      document.getElementById('avgPrice').textContent = data.items.length > 0 ? '$' + (data.totalValue / data.items.length).toFixed(2) : '$0.00';

      // Most valuable item
      if (data.items.length > 0) {
        var mostValuable = data.items.reduce(function(a, b) { return (a.price || 0) > (b.price || 0) ? a : b; });
        document.getElementById('mostValuableItem').textContent = '$' + (mostValuable.price || 0).toFixed(2);
      }

      // Portfolio grade
      var gradeEl = document.getElementById('portfolioGrade');
      var gradeBadge = document.getElementById('gradeBadge');
      var gradeDesc = document.getElementById('gradeDesc');
      var grade = calculateGrade(data.totalValue, data.items.length, data.items);
      gradeBadge.textContent = grade.letter;
      gradeBadge.style.color = grade.color;
      gradeBadge.style.borderColor = grade.color;
      gradeDesc.textContent = grade.description;
      gradeEl.style.display = 'flex';

      // Category breakdown
      var categories = {};
      data.items.forEach(function(item) {
        var cat = item.category || 'Other';
        if (!categories[cat]) categories[cat] = { count: 0, value: 0 };
        categories[cat].count++;
        categories[cat].value += item.price || 0;
      });

      var categoryBreakdown = document.getElementById('categoryBreakdown');
      categoryBreakdown.innerHTML = '';
      Object.entries(categories).sort(function(a, b) { return b[1].value - a[1].value; }).forEach(function(entry) {
        var cat = entry[0], info = entry[1];
        var pct = data.totalValue > 0 ? ((info.value / data.totalValue) * 100).toFixed(0) : 0;
        var card = document.createElement('div');
        card.className = 'il-category-card';
        card.innerHTML = '<span class="il-cat-count">' + info.count + '</span>' +
          '<span class="il-cat-name">' + cat + '</span>' +
          '<span class="il-cat-value">$' + info.value.toFixed(2) + ' (' + pct + '%)</span>';
        categoryBreakdown.appendChild(card);
      });

      // Price tier breakdown
      var tiers = [
        { label: '$500+', min: 500, max: Infinity, color: '#e53935' },
        { label: '$100-500', min: 100, max: 500, color: '#d81b60' },
        { label: '$20-100', min: 20, max: 100, color: '#8e24aa' },
        { label: '$5-20', min: 5, max: 20, color: '#5c6bc0' },
        { label: 'Under $5', min: 0, max: 5, color: '#78909c' },
      ];
      var tierCounts = tiers.map(function(t) {
        return { label: t.label, color: t.color, count: data.items.filter(function(i) { return (i.price || 0) >= t.min && (i.price || 0) < t.max; }).length };
      });
      var maxTier = Math.max.apply(null, tierCounts.map(function(t) { return t.count; }));
      var raritySection = document.getElementById('raritySection');
      var rarityBreakdown = document.getElementById('rarityBreakdown');
      rarityBreakdown.innerHTML = '';
      if (data.items.length > 0) {
        raritySection.style.display = 'block';
        tierCounts.forEach(function(t) {
          var barW = maxTier > 0 ? ((t.count / maxTier) * 100) : 0;
          var row = document.createElement('div');
          row.className = 'il-tier-row';
          row.innerHTML = '<div class="il-tier-label">' + t.label + '</div>' +
            '<div class="il-tier-bar-wrap"><div class="il-tier-bar" style="width:' + barW + '%;background:' + t.color + '"></div></div>' +
            '<div class="il-tier-count" style="color:' + t.color + '">' + t.count + ' items</div>';
          rarityBreakdown.appendChild(row);
        });
      }

      // Top 12 items with skin link
      var topItems = document.getElementById('topItems');
      topItems.innerHTML = '';
      data.items.sort(function(a, b) { return (b.price || 0) - (a.price || 0); }).slice(0, 12).forEach(function(item) {
        var skinSlug = itemNameToSlug(item.name);
        var card = document.createElement('a');
        card.href = skinSlug ? '/cs2-skins/' + skinSlug + '/' : '/cs2-skins/';
        card.className = 'il-item-card';
        card.innerHTML = '<div class="il-item-img">' + escapeHtml(item.name.split('|')[0] || '') + '</div>' +
          '<div class="il-item-info">' +
          '<div class="il-item-name">' + escapeHtml(item.name) + '</div>' +
          '<div class="il-item-wear">' + (item.wear || '-') + '</div></div>' +
          '<div class="il-item-price">$' + (item.price || 0).toFixed(2) + '</div>';
        topItems.appendChild(card);
      });

      // Copy button
      var copyBtn = document.getElementById('copyValueBtn');
      if (copyBtn) {
        copyBtn.onclick = function() {
          navigator.clipboard.writeText('$' + data.totalValue.toFixed(2)).then(function() {
            copyBtn.textContent = 'Copied!';
            setTimeout(function() { copyBtn.textContent = 'Copy Total Value'; }, 2000);
          });
        };
      }

      resultsSection.style.display = 'block';
      resultsSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }

    function calculateGrade(totalValue, itemCount, items) {
      var hasKnifeOrGlove = items.some(function(i) { return i.category === 'Knives' || i.category === 'Gloves'; });
      var highValueCount = items.filter(function(i) { return (i.price || 0) >= 50; }).length;

      if (totalValue >= 10000) return { letter: 'S', color: '#ffd740', description: 'Elite collector inventory. Top-tier portfolio with exceptional value.' };
      if (totalValue >= 5000) return { letter: 'A+', color: '#69f0ae', description: 'Premium portfolio. Strong collection with high-value items.' };
      if (totalValue >= 2000 || (totalValue >= 1000 && hasKnifeOrGlove)) return { letter: 'A', color: '#4fc3f7', description: 'Great collection with solid value. Well-rounded inventory.' };
      if (totalValue >= 500 || highValueCount >= 3) return { letter: 'B+', color: '#81c784', description: 'Good portfolio with growing value. Keep collecting!' };
      if (totalValue >= 200) return { letter: 'B', color: '#aed581', description: 'Decent collection. A few more premium skins would level you up.' };
      if (totalValue >= 50) return { letter: 'C', color: '#ffb74d', description: 'Starter collection. Check our Market Trends for good value picks.' };
      return { letter: 'D', color: '#ef9a9a', description: 'Just getting started. Browse our Best Value skins to build your collection.' };
    }

    function itemNameToSlug(name) {
      if (!name) return '';
      var clean = name.replace(/\s*\(.*?\)\s*$/, '').replace(/^StatTrak\u2122\s+/, '').replace(/^Souvenir\s+/, '');
      return clean.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
    }

    function escapeHtml(str) {
      var d = document.createElement('div');
      d.textContent = str;
      return d.innerHTML;
    }

    function showError(message) {
      errorMessage.textContent = message;
      errorState.style.display = 'block';
      resultsSection.style.display = 'none';
    }
  })();
</script>
